// DEV HOST
const S3_HOST = 'https://s3-ap-northeast-1.amazonaws.com/pixelstairsdev/';
const LOGO_HOST = `${S3_HOST}assets/logo/`;

export const LOGOS = {
    vp: `${LOGO_HOST}vp.svg`,
    text: `${LOGO_HOST}text.svg`
};

export const CUSTOM_HEADER_PREFIX = 'X-pixel-';
